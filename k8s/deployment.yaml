apiVersion: apps/v1
kind: Deployment
metadata:
  name: flask-deployment
spec:
  replicas: 2
  selector:
    matchLabels:
      app: flask
  template:
    metadata:
      labels:
        app: flask
    spec:
      containers:
      - name: flask-container
        image: kalicharandas001/aws-devops-flask-app:v1
        imagePullPolicy: Always
        ports:
        - containerPort: 5000